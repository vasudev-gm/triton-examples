- uses: actions/setup-python@v4
  with:
    python-version: '3.12'
- name: Install PyTorch with CUDA
run: uv pip install torch==2.8.0+cu129 torchaudio==2.8.0+cu129 torchvision==0.23.0+cu129 --index https://download.pytorch.org/whl/cu129
